/** A class to track user data, usually the user currently logged in to user the vrkSpace
 * @class User
 */
class User {
  /** Creates an instance of vrkSpace User to save his/her status
     * @memberof User
     */
  constructor( ) {
    this.type = 'User';
    this.theme = 'intro';
    this.name = 'cybergaukler';
  }
}

export {User};
